import { Typography } from '@material-ui/core';

const Title = () => {
  return (
    <Typography
      style={{ fontFamily: 'Barlow, serif', fontWeight: '500' }}
      variant='h4'
    >
      Chess Simulation Game
    </Typography>
  );
};

export default Title;
